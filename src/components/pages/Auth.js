import React from "react";
import AuthForm from "../Auth/AuthForm";

const Auth = () => {
    return (
        <React.Fragment>
            <AuthForm />
        </React.Fragment>
    );
};

export default Auth;